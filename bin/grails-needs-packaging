#!/bin/bash
#

set -o errexit

function from-code { grails-plugins-from-code | sort; }
function from-work { grails-plugins-from-work-dir | sort; }
function plugins-from-code-not-in-work { comm -23 <(from-code) <(from-work); }

[[ -n "$(plugins-from-code-not-in-work)" ]]
